[project]
name = "brightdata-mcp"
version = "0.1.0"
description = "Fetch recent Instagram/TikTok posts via the BrightData Web Scraper API"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27",
]
